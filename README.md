# Clinical_Pipeline_Docker_integrated

1. Setup instructions: how to run everything locally with Docker
    - First i create a docker-composer.yml file. Through this file i setup docker container and volumns which data store. In local it store your disk
    - Create a Dockerfile to install all dependcies before run code. And it helps to run your code 
    - Using Docker you automate all taks no need manual run every thing
2. Pipeline architecture: each stage and why you structured it that way
   - 
