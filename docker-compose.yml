version: "3.9"

services:
  pipeline:
    build: .
    container_name: clovertex_pipeline

    # run your pipeline
    command: python -m pipeline.main

    # mount local folders
    volumes:
      - ./data:/app/data
      - ./datalake:/app/datalake